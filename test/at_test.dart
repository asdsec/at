import 'dart:io';

import 'package:test/test.dart';

void main() {
  test('calculate', () {
    // final dir = Directory.current;
    // final file = File('${dir.path}\\at.c');
    // expect(isValid(file: file, username: 'asdsec'), false);
  });
}
